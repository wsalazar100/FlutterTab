import 'package:flutter/material.dart';

class LlamadaPagina extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Llamada"),)
    );
  }
}