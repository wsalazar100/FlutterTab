import 'package:flutter/material.dart';

class EstadoPagina extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Estado"),)
    );
  }
}